name: Slither Analysis
on: [push]
jobs:
  analyze:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: 0xCLARITY/slither-action@add-pnpm-support
      # - uses: crytic/slither-action@v0.1.1
